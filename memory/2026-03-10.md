# Daily Notes — 2026-03-10

## Session Summary

Big day — Polymarket integration + two critical bug fixes.

## What We Built

### Polymarket Integration (research-only)
- New `scanner/polymarket_client.py` — fetches 1,500+ active markets via Gamma API
- Scanner now runs Polymarket on every cycle, stores to DB
- Research agent injects Polymarket comparable prices into LLM prompts
- Decision engine and near-term engine both filter `platform == "kalshi"` — Polymarket markets never generate executable opportunities
- Executor has a safety net: hard-rejects any non-Kalshi opportunity

### Bug Fix: Stale crypto prices (ETH $2,041 → $2,350 loss)
- Root cause: `evaluate_near_term()` used LLM web search which returned 2am ETH price at 1:45pm EDT
- Fix 1: KXETHD-/KXBTCD-/KXSOLD- tickers in near-term now route to quant evaluator (live Kraken price)
- Fix 2: Any near-term market mentioning ETH/BTC/SOL gets live Kraken price injected into LLM prompt
- Replay: original trade would have been hard-rejected (margin -2.8% < 5% minimum)

### Bug Fix: Polymarket opportunities reaching executor (Pixelworks PXLW)
- Polymarket market got scored by decision engine (no platform filter), approved, then 404'd on Kalshi
- Fixed: platform filter added to both decision engine queries + executor safety net

### DB upsert fixes
- Deduplication by (external_id, platform) before upsert — Polymarket returns duplicates across pages
- Batch upserts in groups of 200 to stay under PostgreSQL bind-param limit

### Jack workspace repo
- Created https://github.com/alexkartavov/jack-trader for workspace config/memory backup
- .gitignore excludes .secrets/, .openclaw/, pm-trading/ (has its own repo)

## Wins
- Polymarket cross-validation live and working
- ETH stale price bug patched before it could repeat
- All code pushed to both repos (pm-trading + jack-trader)
