# Daily Notes — 2026-03-06

## Session Summary

Long productive day building out the full PM trading system with Alex.

## What We Accomplished

### Infrastructure / DevOps
- Fixed exec approval loop: added `tools.elevated.allowFrom.telegram` to openclaw.json, set `agents.main.security=full` in exec-approvals.json
- Gateway web UI password: `John3:16`
- Fixed Tailwind CSS not loading in dashboard: `postcss.config.mjs` (ES module) conflicted with CommonJS project — replaced with `postcss.config.js`
- Fixed Docker build hanging issues (used `docker cp` to hot-patch containers instead)
- All 5 containers running: pm_worker, pm_backend, pm_scheduler, pm_postgres, pm_redis

### Kalshi Integration
- Auth: RSA-PSS signing working with private key at `/Users/dropdev/.openclaw/workspace/.secrets/kalshi/private.key`
- Correct base URL: `https://api.elections.kalshi.com/trade-api/v2` (not trading-api.kalshi.com)
- Key mounted into Docker at `/run/secrets/kalshi/private.key`
- Balance confirmed: $50.96 (Alex deposited $50)

### Strategy Overhaul (major)
- **Old strategy**: Hunt >95% markets (tiny margins, $0.01 profit per contract)
- **New strategy**: Hunt mispricings — markets where true_prob >> market_price (EV ≥ 12%)
- Scanner now covers 55-90% price range, 7-day resolution window
- Research agent uses Anthropic claude-haiku-4-5 to evaluate "is the market wrong?"
- LLM quota: 30 markets per run, filtered by keyword (skip sports/entertainment)
- Decision engine: 30-min cycle (was 3 min — major token burn reduction)
- Min volume: $2,000 for deep research
- Skip patterns: ` vs `, `winner?`, `billboard`, `price up in next`, etc.
- Research patterns: `will `, `trump say`, `legislation`, `fed `, `cpi`, `temperature`, etc.

### Position Monitoring
- New `monitor_positions` task runs every 20 min
- Exit on: 35% stop-loss, 75% profit capture, time decay (<30min left + underwater)
- `sell_position()` added to KalshiClient

### Re-evaluation
- Previously rejected markets get re-scored after 4 hours (catches data releases)

### Notifications
- Telegram bot token stored in .env: `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID=125381355`
- `notify()` function in `app/notify.py` — fires on trade open

### Dashboard Improvements
- Added: Account balance, scanner stats (markets in DB, active, last scan, countdown to next scan)
- Added: Decision engine panel (last run, next run countdown, total evaluated/approved, approval rate)
- Added: LLM usage (tokens in/out per 24h, estimated cost in USD)
- Numbers formatted: 15K, 3.2M etc.
- `scan_complete` and `decision_complete` SystemEvents logged for timing

## Key Market Finds Today
- Trump "Saving College Sports" roundtable: "Transgender" YES @ 18% — identified as potential mispricing (roundtable is about transgender athletes in college sports), but LLM gave it 45% true prob / 0.62 conf — below threshold. Resolved as correct rejection (market knows more than we do about the agenda).
- Miami weather markets: 83°F at 1:19 PM → 84-85° YES @ 99% was the best play (already near-expired)
- Denver: 32°F at noon with snow → market correctly priced the high at 40-41°

## Risk Settings (for $50 bankroll)
- MAX_BET_SIZE: $10
- MAX_TOTAL_EXPOSURE: $40
- MAX_DAILY_LOSS: $20

## Current Status (9:48 PM ET)
- System running autonomously, 0 trades placed yet
- 15,626 markets evaluated, 0 approved (LLM correctly conservative)
- Decision engine next run: ~30 min cycle
- Waiting for right opportunities: data releases, observed weather, strongly telegraphed political events
- TODO: first trade notification via Telegram when it fires

## Technical Debt / Known Issues
- Docker builds hang intermittently (use `docker cp` + `docker restart` as workaround)
- `decision_complete` events only logged after tonight's changes (no historical data yet)
- Opportunities page in dashboard shows `status=approved` by default — currently always empty
